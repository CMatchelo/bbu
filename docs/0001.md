# Project explanation
This is a university basketball manager gamer that takes places in Brazil.
There are 5 leagues with 10 teams each.
Each of the 49 teams not used by the players can have up to 15 players, 3 in each position (C, PF, SF, SG, PG)

## First test
When running this, create a plan of what you`re going to execute in order to create the feature described in this file.
After creating a plan, show it to me for validation.
After validation, start building as agreed.

## Draft play
1. The user type, that is in the context useUser() has the attribute user.currentWeek.
2. When currentWeek is equal to 1 or 25, (create these as a constant in constant folder), it will occur a "inside draft
3. In team page (src/pages/team), when this page is access for the first time either in week 1 or 25, display a popup telling the user about a inside selection.
  a. Thie selection should work as a player selection among the new students.
4. Is this popup, display the 10 players generated and returned in the generateDraftPlayers function, in the file src/game/createPlayer.ts
5. The user will be able to select players until the team has 15 players at max.
6. After the user select the players, call the function addDraftedPlayers in the file src/game/createPlayer.ts
7. Use the response of the function to dispatch
  dispatch(setPlayers(players));
  dispatch(setUniversities(updatedUnis));
8. AFter that, save the files using the functions in utils/saveGame.ts
