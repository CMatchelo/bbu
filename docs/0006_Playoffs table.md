### Scouting

1. We should have a Playoffs tab in side menu, so that the user will be able to see the matchups.

## Config

For this spec, use branch CL-0005/Playoffs, already created and active in vscode
Always create a plan and present it to me before running any changes
If any questions, ask before deciding anything

## What to do

1. Add, under "Liga" section, a button to go to "Playoffs" page
2. If the playoffs havent started yet, display a "Regular season ongoing" message or similar
3. If the playoffs start

- DIsplay, on top, a menu where the user can select which round he wants to see
- Display a table showing each matchup, and results, of the selected round
- Each matchup card should have
  - Teams seed, ID, nickname, and all the results
  - The winner should be highlighted if the matchup already ended
  - The winner of each match should have the value of its point in green

## Screen/Taiwlind changes

1. THe following pages have a top menu, they all should follow the same pattern, that is the one in "Scout"
 - Team (fix)
 - Scout, REcruiting Board, Commitments (already in pattern)
 - Leagues (fix)
2. The top menu should use the colors highlights2, highlights2dark and highlights2light
3. Scout, REcruiting Board, Commitments have, on side of the top menu, the title of the page, add similiar to all other pages but following:
  - The title should be centrilized, above the top menu (if the menu exists)
4. Page Practice and table education under page Team, both have a button to save, move to top-right, similiar to the on in "Scout"
5. Under Medical Center page and league Injuries page, display a message of "No inguries" or similar, when there is no players injured to display


## Fixes

1. No need

## Renaming and refactor

1. No need

## Libraries

1. No need
