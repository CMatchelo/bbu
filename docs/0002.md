## Config
For this spec, use branch CL-0002
Always create a plan and present it to me before running the changes

## Feature / Problem
When it is a new game, the user may not undestand whats going won, neither know its current team.

## What to do
1. At the top of the table, add an explanation that at the start of every year, the team might run a "inside draft" among the new students so to fill the team if there are empty spots.
2. Do not display all the skills for the new players, neither the overall
 - Use the calculatMaxMinGrade function inside createPLayer, to display a range for the overall.
 - Use the same to calculate the range for the potential
 - Display also a table with the current roster
 - Do not display as a POPUP anymore, display as a component in the index, such as eduTable, skillsTable
3. Also, change the name of the table to "Prospects"